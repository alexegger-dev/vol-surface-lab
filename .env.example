# Backend (optional; defaults are safe for local dev)
APP_ENV=development
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
MAX_UPLOAD_BYTES=5242880

# Frontend (build-time in Docker; dev server reads at start)
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
